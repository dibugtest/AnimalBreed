@model IEnumerable<Animal.Models.ViewModel.GoatIndexVM>

@{
    ViewData["Title"] = "Index";
}
<div class="card mb-4">
    <div class="card-header py-3">
        <h3 class="m-1 font-weight-bold center text-primary">बाख्राहरु</h3>
    </div>
    <div class="card-body">
        <p>
            <a asp-action="AddEditAnimal" class="btn btn-primary">Create New</a>
        </p>
        <div class="row">
            @*<div class="col-md-3">
                    <div class="form-group">
                        <label></label>
                        @Html.DropDownList("breed", Model.breed, "Select Breed", new { @class = "form-control", id = "ddlBreed" })
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label></label>
                        @Html.DropDownList("species", Model.species, "Select Species", new { @class = "form-control", id = "ddlSpecies" })
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label></label>
                        @Html.DropDownList("owner", Model.owner, "Select Owner", new { @class = "form-control", id = "ddlOwner" })
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label></label>
                        @Html.DropDownList("farm", Model.farm, "Select Farm", new { @class = "form-control", id = "ddlFarm" })
                    </div>
                </div>*@
        </div>
        @*<label>@ViewData["fileLocation"]</label>*@

        <div style="padding-top:10px;">
            <table class="table table-bordered" id="myTable">
                <thead>
                    <tr>
                        <th>
                            जात
                        </th>
                        <th>
                            Origin of
                        </th>
                        <th>
                           हावापानी
                        </th>
                        <th>
                           उपयुक्त
                        </th>
                        <th>
                          खरिद मूल्य
                        </th>
                        <th>
                          बिउ बोकाको मूल्य
                        </th>
                        <th>
                          वृद्धिदर
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>

                            <td>
                                @Html.DisplayFor(modelItem => item.Breed.BreedName)
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.Goat.OriginOf)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Goat.Weather)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Goat.MainPurpose)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Goat.PurchasePerKg)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Goat.PricePerKgDam)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Goat.GrowthRateWt)
                            </td>

                            <td>
                                @Html.ActionLink("Edit", "AddEditAnimal", new { id = item.Goat.id }, new { @class = "btn btn-primary" }) |
                                @Html.ActionLink("Details", "Details", new { id = item.Goat.id }, new { @class = "btn btn-info" }) |
                                @Html.ActionLink("Delete", "DeleteAnimal", new { id = item.Goat.id }, new { @class = "btn btn-success" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $("body div.myTable input");
            var dataSrc = [];
            var table = $("#myTable").DataTable({
                "columnDefs": [
                    { "orderable": false, "targets": 3 }
                ],
                "pageLength": 50,
            });

            //$("#ddlBreed").change(function () {
            //    searchData(2, $(this).val());
            //})

            //$("#ddlSpecies").change(function () {
            //    searchData(1, $(this).val());
            //})

            //$("#ddlFarm").change(function () {
            //    searchData(6, $(this).val());
            //})

            //$("#ddlOwner").change(function () {
            //    searchData(5, $(this).val());
            //})


        });

                //function searchData(col, value) {
                //    var table = $("#myTable").DataTable();
                //    table.column(col).search(value).draw();
                //}
    </script>
}
